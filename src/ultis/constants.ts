enum TYPES {
  all = 'all',
  movie = 'movie',
  series = 'series',
  episode = 'episode',
}

enum PAGINATION {
  ROW = 10,
  PAGE = '1',
}

enum PLOT {
  FULL = 'full',
}

export { TYPES, PAGINATION, PLOT };
